import HomeScreen from '../page/home'

const RouteConfig = {
    Home: {
        screen: HomeScreen,
        //navigationOptions: ({navigation}) => ({header: null})
    }
}

export default RouteConfig;